hooks/health-check
#!/bin/sh
 
# define default return code as 0
rc=0
curl localhost:8080/national-parks | grep "National Perks"
case $? in
  0)
    rc=1 ;;
  3)
    rc=0 ;;
  4)
    rc=2 ;;
  *)
    rc=3 ;;
esac
 
exit $rc
